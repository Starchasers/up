module.exports = {
  handleImages: ['jpe', 'jpeg', 'png', 'svg'],
  // node_modules directory is checked for types, need help with that how to ignore it
  typescript: {
    ignoreBuildErrors: true,
  },
}
