import styled from '@emotion/styled'

const LineWithTextContainer = styled('div')`
  display: grid;
  grid-template-columns: 1fr auto 1fr;
  grid-column-gap: 10px;
  margin: 25px 0;
  align-items: center;
  width: 100%;
`

export default LineWithTextContainer
