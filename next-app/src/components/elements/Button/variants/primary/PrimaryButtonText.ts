import styled from '@emotion/styled'

import ButtonText from '../../ButtonText'

const PrimaryButtonText = styled(ButtonText)`
  color: ${(props) => props.theme.colors.upBase03};
  font-weight: normal;
  line-height: 1;
`

export default PrimaryButtonText
