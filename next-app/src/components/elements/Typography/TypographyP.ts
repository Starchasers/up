import styled from '@emotion/styled'

const TypographyP = styled('p')`
  color: ${(props) => props.theme.colors.upNeutralFillNight};
  font-weight: normal;
  font-size: 13px;
`

export default TypographyP
